---
phase: 06-templates-onboarding
plan: 04
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - lib/core/router/app_router.dart
  - lib/features/onboarding/presentation/onboarding_flow.dart
  - lib/app.dart
autonomous: true
must_haves:
  truths:
    - GoRouter configuration lives in lib/core/router/app_router.dart
    - GoRouter uses Riverpod (riverpod_annotation) — router provider is @riverpod
    - Top-level routes: /onboarding (ShellRoute with OnboardingFlow), /home (placeholder)
    - Onboarding sub-routes: /onboarding/condition, /onboarding/template, /onboarding/anchors, /onboarding/medicines, /onboarding/review, /onboarding/permissions
    - Initial location is /onboarding when isComplete is false, /home when true
    - OnboardingFlow is a ShellRoute builder — wraps step screens in a Scaffold with progress indicator
    - OnboardingFlow shows a linear progress bar based on currentStep ordinal / total steps
    - OnboardingFlow scaffold has a back button that navigates to the previous step (except on condition step)
    - app.dart creates MaterialApp.router with GoRouter.routerDelegate and routeInformationParser
    - app.dart sets the Theme with teal seed color, Noto Sans font, minimum 18pt body text
    - Redirect guard: if onboarding is complete, redirect /onboarding/* to /home
    - Redirect guard: if onboarding is NOT complete, redirect /home to /onboarding/condition
    - All onboarding routes use GoRouterState to pass data if needed
    - onboarding_flow.dart REPLACES the existing placeholder
    - app_router.dart is NEW
    - app.dart is NEW (or REPLACES existing placeholder if exists)
  artifacts:
    - lib/core/router/app_router.dart — GoRouter configuration with onboarding + home routes
    - lib/features/onboarding/presentation/onboarding_flow.dart — ShellRoute builder with progress indicator
    - lib/app.dart — MaterialApp.router with theme + GoRouter
  key_links:
    - .planning/research/ARCHITECTURE.md — Section 3 project structure (app.dart location)
    - .planning/research/PITFALLS.md — P-UX-05 (reduce onboarding steps), P-07 (18pt font, high contrast)
    - .planning/REQUIREMENTS.md — ONBD-01 (flow step order), ONBD-05 (18pt text, high contrast)
    - lib/features/onboarding/application/onboarding_notifier.dart — OnboardingNotifier provider (from Plan 06-02)
    - lib/features/onboarding/domain/models/onboarding_state.dart — OnboardingStep enum (from Plan 06-02)
---

<objective>
Set up GoRouter for the entire app with the onboarding flow as the initial experience.

GoRouter is configured with two top-level route groups: /onboarding (ShellRoute) and /home. A redirect guard checks OnboardingNotifier.isComplete to route users appropriately.

OnboardingFlow is the ShellRoute builder that wraps each step screen in a consistent Scaffold with a linear progress bar, back navigation, and accessibility-compliant styling.

app.dart wires everything together: MaterialApp.router with the teal theme, Noto Sans font, 18pt minimum body text, and high-contrast colors per ONBD-05 and P-07.

Output: 1 new file (app_router.dart) + 1 replaced placeholder (onboarding_flow.dart) + 1 new file (app.dart).
</objective>

<execution_context>
@.planning/phases/06-templates-onboarding/06-04-PLAN.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md (Section 3 — app.dart, GoRouter)
@.planning/research/PITFALLS.md (P-UX-05, P-07, P-UX-07)
@.planning/REQUIREMENTS.md (ONBD-01, ONBD-05)

**What exists:**

- `lib/features/onboarding/presentation/onboarding_flow.dart` — placeholder with TODO, REPLACE
- `lib/app.dart` — may or may not exist yet. If not, CREATE. If placeholder, REPLACE.
- GoRouter is in pubspec.yaml (go_router package installed)
- Google Fonts installed (google_fonts package)

**OnboardingStep enum (from Plan 06-02):**

```dart
enum OnboardingStep {
  condition,   // /onboarding/condition
  template,    // /onboarding/template
  anchors,     // /onboarding/anchors
  medicines,   // /onboarding/medicines
  review,      // /onboarding/review
  permissions, // /onboarding/permissions
  complete,    // redirect to /home
}
```

**Route → Step mapping:**
| Route | Step | Screen |
|---------------------------|-------------|--------------------------|
| /onboarding/condition | condition | ConditionStep (06-05) |
| /onboarding/template | template | TemplatePicker (06-05) |
| /onboarding/anchors | anchors | AnchorStep (06-06) |
| /onboarding/medicines | medicines | MedicineStep (06-06) |
| /onboarding/review | review | ReviewStep (06-07) |
| /onboarding/permissions | permissions | PermissionStep (06-07) |

**Accessibility theme (ONBD-05 + P-07):**

- Teal seed color: `Color(0xFF009688)` or `Colors.teal`
- Font: Noto Sans (via google_fonts)
- Body text minimum: 18pt (fontSize: 18)
- Title text: 24pt+
- Touch targets: 56dp minimum
- High contrast: WCAG AAA (7:1) for text on backgrounds

**Progress indicator:**

```
Step 1 of 6 ████░░░░░░░░░░░░  Condition
Step 2 of 6 ████████░░░░░░░░  Template
Step 3 of 6 ████████████░░░░  Anchors
...
```

Use LinearProgressIndicator with value = (currentStepIndex + 1) / totalVisibleSteps.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create GoRouter configuration with onboarding routes</name>
  <files>lib/core/router/app_router.dart</files>
  <action>
CREATE directory `lib/core/router/` and file `app_router.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';
import 'package:memo_care/features/onboarding/application/onboarding_notifier.dart';
import 'package:memo_care/features/onboarding/domain/models/onboarding_state.dart';
import 'package:memo_care/features/onboarding/presentation/onboarding_flow.dart';
import 'package:memo_care/features/onboarding/presentation/condition_step.dart';
import 'package:memo_care/features/onboarding/presentation/anchor_step.dart';
import 'package:memo_care/features/onboarding/presentation/medicine_step.dart';
import 'package:memo_care/features/onboarding/presentation/review_step.dart';
import 'package:memo_care/features/templates/presentation/template_picker_screen.dart';

part 'app_router.g.dart';

/// Route path constants.
abstract final class AppRoutes {
  static const onboarding = '/onboarding';
  static const condition = 'condition';
  static const template = 'template';
  static const anchors = 'anchors';
  static const medicines = 'medicines';
  static const review = 'review';
  static const permissions = 'permissions';
  static const home = '/home';
}

@riverpod
GoRouter appRouter(AppRouterRef ref) {
  final onboardingState = ref.watch(onboardingNotifierProvider);

  return GoRouter(
    initialLocation: onboardingState.isComplete
        ? AppRoutes.home
        : '${AppRoutes.onboarding}/${AppRoutes.condition}',
    redirect: (context, state) {
      final isOnboarding = state.uri.path.startsWith(AppRoutes.onboarding);
      final isComplete = onboardingState.isComplete;

      // If onboarding complete, redirect away from onboarding
      if (isComplete && isOnboarding) {
        return AppRoutes.home;
      }

      // If onboarding NOT complete, redirect to onboarding
      if (!isComplete && !isOnboarding) {
        return '${AppRoutes.onboarding}/${AppRoutes.condition}';
      }

      return null; // no redirect
    },
    routes: [
      // Onboarding flow (ShellRoute wraps all steps)
      ShellRoute(
        builder: (context, state, child) => OnboardingFlow(child: child),
        routes: [
          GoRoute(
            path: '${AppRoutes.onboarding}/${AppRoutes.condition}',
            builder: (context, state) => const ConditionStep(),
          ),
          GoRoute(
            path: '${AppRoutes.onboarding}/${AppRoutes.template}',
            builder: (context, state) => const TemplatePickerScreen(),
          ),
          GoRoute(
            path: '${AppRoutes.onboarding}/${AppRoutes.anchors}',
            builder: (context, state) => const AnchorStep(),
          ),
          GoRoute(
            path: '${AppRoutes.onboarding}/${AppRoutes.medicines}',
            builder: (context, state) => const MedicineStep(),
          ),
          GoRoute(
            path: '${AppRoutes.onboarding}/${AppRoutes.review}',
            builder: (context, state) => const ReviewStep(),
          ),
          GoRoute(
            path: '${AppRoutes.onboarding}/${AppRoutes.permissions}',
            builder: (context, state) => const PermissionStep(),
          ),
        ],
      ),

      // Home (placeholder — Phase 07 builds the real daily view)
      GoRoute(
        path: AppRoutes.home,
        builder: (context, state) => const Scaffold(
          body: Center(child: Text('Home — built in Phase 07')),
        ),
      ),
    ],
  );
}
```

Note: `PermissionStep` will be created in Plan 06-07. For now, add a placeholder import comment. The file will compile fully after all plans are executed.

Adjust imports for the actual screen file paths. The screen widgets (ConditionStep, TemplatePickerScreen, AnchorStep, MedicineStep, ReviewStep) are created in Plans 06-05, 06-06, 06-07. The router compiles last via wave ordering.

Verify:

- `dart run build_runner build --delete-conflicting-outputs` (generates .g.dart)
- All route paths start with /onboarding/
- Redirect logic handles both directions
  </action>
  </task>

<task type="auto">
  <name>Task 2: Create OnboardingFlow shell + app.dart with theme</name>
  <files>lib/features/onboarding/presentation/onboarding_flow.dart, lib/app.dart</files>
  <action>
REPLACE the entire contents of `lib/features/onboarding/presentation/onboarding_flow.dart` with:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:memo_care/features/onboarding/application/onboarding_notifier.dart';
import 'package:memo_care/features/onboarding/domain/models/onboarding_state.dart';
import 'package:memo_care/core/router/app_router.dart';

/// Shell wrapper for onboarding step screens.
///
/// Provides:
/// - Linear progress indicator showing current step
/// - Back navigation (except on first step)
/// - Consistent Scaffold with accessibility-compliant padding
///
/// Used as the ShellRoute builder in [appRouterProvider].
class OnboardingFlow extends ConsumerWidget {
  const OnboardingFlow({required this.child, super.key});

  final Widget child;

  /// Steps visible in the progress bar (excludes 'complete').
  static const _visibleSteps = [
    OnboardingStep.condition,
    OnboardingStep.template,
    OnboardingStep.anchors,
    OnboardingStep.medicines,
    OnboardingStep.review,
    OnboardingStep.permissions,
  ];

  /// Maps each step to its GoRouter path for back navigation.
  static const _stepPaths = {
    OnboardingStep.condition: '${AppRoutes.onboarding}/${AppRoutes.condition}',
    OnboardingStep.template: '${AppRoutes.onboarding}/${AppRoutes.template}',
    OnboardingStep.anchors: '${AppRoutes.onboarding}/${AppRoutes.anchors}',
    OnboardingStep.medicines: '${AppRoutes.onboarding}/${AppRoutes.medicines}',
    OnboardingStep.review: '${AppRoutes.onboarding}/${AppRoutes.review}',
    OnboardingStep.permissions: '${AppRoutes.onboarding}/${AppRoutes.permissions}',
  };

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final state = ref.watch(onboardingNotifierProvider);
    final currentIndex = _visibleSteps.indexOf(state.currentStep);
    final progress = (currentIndex + 1) / _visibleSteps.length;
    final isFirstStep = currentIndex <= 0;

    return Scaffold(
      appBar: AppBar(
        leading: isFirstStep
            ? null
            : IconButton(
                icon: const Icon(Icons.arrow_back),
                iconSize: 28,
                tooltip: 'Go back',
                onPressed: () {
                  if (currentIndex > 0) {
                    final prevStep = _visibleSteps[currentIndex - 1];
                    final prevPath = _stepPaths[prevStep];
                    if (prevPath != null) {
                      ref.read(onboardingNotifierProvider.notifier).goToStep(prevStep);
                      context.go(prevPath);
                    }
                  }
                },
              ),
        title: Text(
          'Step ${currentIndex + 1} of ${_visibleSteps.length}',
          style: const TextStyle(fontSize: 18),
        ),
        centerTitle: true,
      ),
      body: Column(
        children: [
          // Progress bar
          LinearProgressIndicator(
            value: progress.clamp(0.0, 1.0),
            minHeight: 6,
            semanticsLabel: 'Onboarding progress',
            semanticsValue: '${((progress) * 100).round()}%',
          ),
          // Step content
          Expanded(
            child: SafeArea(
              child: Padding(
                padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 16),
                child: child,
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

CREATE (or REPLACE) `lib/app.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:google_fonts/google_fonts.dart';
import 'package:memo_care/core/router/app_router.dart';

/// Root application widget.
///
/// Configures:
/// - GoRouter for navigation
/// - Teal-seeded Material 3 theme
/// - Noto Sans font for Hindi compatibility
/// - Minimum 18pt body text (ONBD-05)
/// - High-contrast colors (P-07)
class MemoCareApp extends ConsumerWidget {
  const MemoCareApp({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(appRouterProvider);

    return MaterialApp.router(
      title: 'MemoCare',
      debugShowCheckedModeBanner: false,
      theme: _buildTheme(),
      routerConfig: router,
    );
  }

  ThemeData _buildTheme() {
    final colorScheme = ColorScheme.fromSeed(
      seedColor: Colors.teal,
      brightness: Brightness.light,
    );

    final textTheme = GoogleFonts.notoSansTextTheme().copyWith(
      // Enforce minimum 18pt for body text (ONBD-05)
      bodyLarge: GoogleFonts.notoSans(fontSize: 20, height: 1.5),
      bodyMedium: GoogleFonts.notoSans(fontSize: 18, height: 1.4),
      bodySmall: GoogleFonts.notoSans(fontSize: 18, height: 1.4),
      // Titles are 24pt+ for readability
      titleLarge: GoogleFonts.notoSans(fontSize: 28, fontWeight: FontWeight.bold),
      titleMedium: GoogleFonts.notoSans(fontSize: 24, fontWeight: FontWeight.w600),
      titleSmall: GoogleFonts.notoSans(fontSize: 20, fontWeight: FontWeight.w600),
      // Labels for buttons — 18pt minimum
      labelLarge: GoogleFonts.notoSans(fontSize: 20, fontWeight: FontWeight.w600),
      labelMedium: GoogleFonts.notoSans(fontSize: 18, fontWeight: FontWeight.w500),
      labelSmall: GoogleFonts.notoSans(fontSize: 18, fontWeight: FontWeight.w500),
    );

    return ThemeData(
      useMaterial3: true,
      colorScheme: colorScheme,
      textTheme: textTheme,

      // Enforce 56dp minimum touch targets (P-07)
      elevatedButtonTheme: ElevatedButtonThemeData(
        style: ElevatedButton.styleFrom(
          minimumSize: const Size(double.infinity, 56),
          textStyle: GoogleFonts.notoSans(fontSize: 20, fontWeight: FontWeight.w600),
          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
        ),
      ),
      outlinedButtonTheme: OutlinedButtonThemeData(
        style: OutlinedButton.styleFrom(
          minimumSize: const Size(double.infinity, 56),
          textStyle: GoogleFonts.notoSans(fontSize: 20, fontWeight: FontWeight.w600),
          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
        ),
      ),
      textButtonTheme: TextButtonThemeData(
        style: TextButton.styleFrom(
          minimumSize: const Size(48, 56),
          textStyle: GoogleFonts.notoSans(fontSize: 18),
        ),
      ),

      // Card styling for template cards, medicine cards
      cardTheme: CardTheme(
        elevation: 2,
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
        margin: const EdgeInsets.symmetric(vertical: 8),
      ),

      // Input fields (medicine name, dosage)
      inputDecorationTheme: InputDecorationTheme(
        border: OutlineInputBorder(borderRadius: BorderRadius.circular(12)),
        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 16),
        labelStyle: GoogleFonts.notoSans(fontSize: 18),
        hintStyle: GoogleFonts.notoSans(fontSize: 18),
      ),

      // AppBar
      appBarTheme: AppBarTheme(
        centerTitle: true,
        titleTextStyle: GoogleFonts.notoSans(
          fontSize: 20,
          fontWeight: FontWeight.w600,
          color: colorScheme.onSurface,
        ),
      ),
    );
  }
}
```

Update `lib/main.dart` to use `MemoCareApp`:

In main.dart, ensure the `runApp` call wraps `MemoCareApp` in a `ProviderScope`:

```dart
void main() {
  runApp(const ProviderScope(child: MemoCareApp()));
}
```

Verify:

- `flutter analyze` passes
- Theme enforces 18pt minimum across all text styles
- All button minimumSize is ≥56dp
  </action>
  </task>
  </tasks>
