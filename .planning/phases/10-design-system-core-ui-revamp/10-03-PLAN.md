---
phase: 10-design-system-core-ui-revamp
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/features/daily_schedule/presentation/todays_full_schedule_screen.dart
  - lib/features/daily_schedule/presentation/widgets/schedule_status_chips.dart
  - lib/features/daily_schedule/presentation/widgets/hourly_timeline_item.dart
  - lib/features/daily_schedule/presentation/widgets/chain_group_header.dart
  - lib/features/daily_schedule/presentation/widgets/schedule_empty_state.dart
  - lib/features/daily_schedule/application/full_schedule_providers.dart
  - lib/core/router/app_router.dart
autonomous: true
must_haves:
  truths:
    # --- Screen Layout ---
    - TodaysFullScheduleScreen is a ConsumerWidget mounted at /schedule route (shell branch index 1)
    - AppBar shows "Today's Schedule" title with date subtitle
    - Below AppBar: horizontal row of ScheduleStatusChips showing Done/Pending/Missed counts as colored pill chips
    - Main body: scrollable list of HourlyTimelineItem widgets, grouped by hour
    # --- Status Chips ---
    - ScheduleStatusChips is a Row of 3 chip widgets: "X Done" (green), "Y Pending" (amber), "Z Missed" (red)
    - Each chip has colored background (10% opacity) + colored text + count
    - Chip shape: pill (AppSpacing.chipRadius = 999)
    # --- Hourly Timeline ---
    - HourlyTimelineItem has: time label on left (14pt, grey), vertical colored border strip (4px wide, status color), content card on right
    - Content card shows: medicine name (17pt), dosage, chain indicator (lock icon if part of chain), status badge
    - Cards with chain relationships are grouped: ChainGroupHeader shows chain name with link icon
    - Between groups: a subtle divider with chain connector line
    # --- Chain Grouping ---
    - ChainGroupHeader shows chain name, link icon, "X of Y complete" subtitle
    - Lock icon appears on nodes that are blocked by upstream incomplete
    - Chain connectors: thin dashed line connecting grouped items vertically
    # --- Empty State ---
    - ScheduleEmptyState shows tea cup illustration (or placeholder icon), "Rest of the day is free" text (from docx P5)
    - Shown when all reminders for today are complete, or when no reminders exist
    # --- Providers ---
    - fullScheduleProvider groups today's reminders by hour and chain
    - Watches existing dailyScheduleNotifierProvider and transforms data
    # --- Design Tokens ---
    - All colors/typography/spacing from 10-01 design system
  artifacts:
    - lib/features/daily_schedule/presentation/todays_full_schedule_screen.dart — NEW
    - lib/features/daily_schedule/presentation/widgets/schedule_status_chips.dart — NEW
    - lib/features/daily_schedule/presentation/widgets/hourly_timeline_item.dart — NEW
    - lib/features/daily_schedule/presentation/widgets/chain_group_header.dart — NEW
    - lib/features/daily_schedule/presentation/widgets/schedule_empty_state.dart — NEW
    - lib/features/daily_schedule/application/full_schedule_providers.dart — NEW
    - lib/core/router/app_router.dart — UPDATED, /schedule branch now renders TodaysFullScheduleScreen
  key_links:
    - .planning/design-reference/todays-full-schedule.html — Primary visual spec
    - docs/MemoCare_UI_UX_Prompts.txt — P5 (Today's Full Schedule)
    - .planning/PLANNING_RULES.md — Rule 13 (empty states)
---

<objective>
Build the new Today's Full Schedule screen — a dedicated view showing all reminders for today in an hourly timeline format with status chips, chain grouping, and colored border strips.

This is a completely NEW screen (no existing equivalent). Maps to the /schedule tab (index 1 in bottom nav from 10-01).

Reference: `todays-full-schedule.html` (Stitch) + P5 (docx)

Output: Complete schedule screen with hourly timeline, status summary chips, chain grouping, and empty state.
</objective>

<execution_context>
@.planning/phases/10-design-system-core-ui-revamp/10-03-PLAN.md
</execution_context>

<context>
@.planning/design-reference/todays-full-schedule.html — Visual spec
@docs/MemoCare_UI_UX_Prompts.txt — P5 behavioral/layout spec

**What 10-01 provides:** Design tokens, /schedule route (currently placeholder)
**What exists:** DailyScheduleNotifier provides today's reminders. No dedicated schedule screen exists yet — home screen shows a basic list.

**Key new features:**

- Hourly timeline layout (8:00 AM section, 9:00 AM section, etc.)
- Summary status chips (Done X / Pending Y / Missed Z)
- Chain grouping with lock icons for blocked nodes
- Empty state: "Rest of the day is free" with tea cup illustration
  </context>

<steps>
1. Create ScheduleStatusChips: Row of 3 pill chips showing done/pending/missed counts with colored backgrounds.

2. Create HourlyTimelineItem: Row with time label (left), colored border strip (4px, centered), content card (right). Content card has medicine name, dosage, chain info, status badge.

3. Create ChainGroupHeader: Shows chain name with link icon and "X of Y complete" progress. Groups related reminders visually.

4. Create ScheduleEmptyState: Centered column with icon placeholder and "Rest of the day is free" text (from docx P5 empty state spec).

5. Create fullScheduleProvider: Watches dailyScheduleNotifierProvider, groups reminders by hour, sorts within each hour, annotates with chain membership.

6. Build TodaysFullScheduleScreen: AppBar with title + date → ScheduleStatusChips → ScrollView of hourly sections with HourlyTimelineItem widgets. Show ScheduleEmptyState when appropriate.

7. Update AppRouter: Replace /schedule placeholder with TodaysFullScheduleScreen.

8. Widget tests: Verify screen renders status chips, timeline items, chain headers, and empty state.
   </steps>
