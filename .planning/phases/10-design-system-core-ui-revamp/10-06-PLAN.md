---
phase: 10-design-system-core-ui-revamp
plan: 06
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/features/templates/presentation/template_library_screen.dart
  - lib/features/templates/presentation/widgets/template_search_bar.dart
  - lib/features/templates/presentation/widgets/category_filter_chips.dart
  - lib/features/templates/presentation/widgets/template_card.dart
  - lib/features/templates/presentation/widgets/template_detail_sheet.dart
  - lib/features/templates/presentation/widgets/course_tracker_badge.dart
  - lib/features/templates/presentation/template_picker_screen.dart
  - lib/features/templates/domain/template_pack.dart
  - lib/features/templates/data/template_packs_data.dart
  - lib/features/templates/application/template_providers.dart
  - lib/core/router/app_router.dart
autonomous: true
must_haves:
  truths:
    # --- Template Library Screen (NEW) ---
    - TemplateLibraryScreen is a standalone browsable screen (NOT the onboarding picker)
    - Accessible via navigation or deep link /templates
    - AppBar: "Templates" title
    - Below AppBar: TemplateSearchBar (text input with search icon, 12px radius)
    - Below search: CategoryFilterChips horizontal scroll (All, Diabetes, Blood Pressure, Heart, Hydration, Wellness, Eye Care, School)
    - Grid of TemplateCard widgets (2 columns, 120pt height cards)
    # --- Template Card ---
    - TemplateCard: 120pt height, 16px radius, white background, subtle shadow
    - Top: colored icon circle (40px, category-specific color)
    - Title: template name (17pt, max 2 lines)
    - Bottom: tag pills (pill shape, category label) + course tracker badge if applicable
    - Tapping opens TemplateDetailSheet as modal bottom sheet
    # --- Template Detail Sheet ---
    - TemplateDetailSheet: DraggableScrollableSheet / showModalBottomSheet
    - Header: template name (22pt SemiBold), category tag pill, description text
    - Content: list of medicines in the template with dosages and timing
    - ChainVisualization: simplified vertical list showing chain relationships
    - Bottom: "Apply Template" primary button (full-width, 56px height, AppColors.accent)
    - "Customize First" secondary button for editing before applying
    # --- Course Tracker Badge ---
    - CourseTrackerBadge: small pill showing "Day X of Y" for course-based templates
    - Used on templates that have a defined duration (e.g., antibiotics 7-day course)
    # --- Search + Filter ---
    - Search filters templates by name (case-insensitive substring match)
    - Category chips filter by category enum
    - Combined: search AND category filter apply together
    # --- New Template Packs ---
    - Add 4 new packs: Hydration Booster, Heart Patient, Elderly Wellness, Eye Care (Rule 9)
    - Each pack: name, category, description, icon, color, list of items with doses/times/chain relationships
    # --- Existing Picker Updates ---
    - TemplatePickerScreen (onboarding): Updated to use new TemplateCard design
    - Condition-filtered (only shows relevant templates for selected condition)
    # --- Design Tokens ---
    - All colors/typography/spacing from 10-01
  artifacts:
    - lib/features/templates/presentation/template_library_screen.dart — NEW, standalone browsable
    - lib/features/templates/presentation/widgets/template_search_bar.dart — NEW
    - lib/features/templates/presentation/widgets/category_filter_chips.dart — NEW
    - lib/features/templates/presentation/widgets/template_card.dart — NEW, 120pt card design
    - lib/features/templates/presentation/widgets/template_detail_sheet.dart — NEW, modal bottom sheet
    - lib/features/templates/presentation/widgets/course_tracker_badge.dart — NEW
    - lib/features/templates/presentation/template_picker_screen.dart — UPDATED, new card design
    - lib/features/templates/domain/template_pack.dart — UPDATED with new categories
    - lib/features/templates/data/template_packs_data.dart — UPDATED with 4 new packs
    - lib/features/templates/application/template_providers.dart — UPDATED with search + filter
    - lib/core/router/app_router.dart — UPDATED with /templates route
  key_links:
    - .planning/design-reference/template-library.html — Library screen spec
    - .planning/design-reference/template-detail-sheet.html — Detail sheet spec
    - docs/MemoCare_UI_UX_Prompts.txt — P6 (Template Library)
    - .planning/PLANNING_RULES.md — Rule 9 (8 template packs)
---

<objective>
Build the standalone Template Library screen with search, category filters, grid cards, and detail bottom sheet. Add 4 new template packs (Hydration Booster, Heart Patient, Elderly Wellness, Eye Care). Add course tracker badge.

Reference: `template-library.html` + `template-detail-sheet.html` (Stitch) + P6 (docx)

Output: Complete template library with search/filter, new packs, detail sheet, and course tracker.
</objective>

<execution_context>
@.planning/phases/10-design-system-core-ui-revamp/10-06-PLAN.md
</execution_context>

<context>
@.planning/design-reference/template-library.html
@.planning/design-reference/template-detail-sheet.html
@docs/MemoCare_UI_UX_Prompts.txt — P6

**Current state:** TemplatePickerScreen exists for onboarding only. Shows 3 template packs (Diabetic, BP, School) as simple list tiles. No standalone library, no search, no filters, no detail sheet, no course tracker.

**Key additions:**

- Standalone /templates route (browsable outside onboarding)
- Search bar with live filtering
- Category filter chips (horizontal scroll)
- Grid of redesigned template cards (120pt, icon circle, tags)
- Modal bottom sheet for template details
- 4 new template packs
- Course tracker badge (Day X of Y)
  </context>

<steps>
1. Add 4 new TemplatePack definitions to template_packs_data.dart: Hydration Booster, Heart Patient, Elderly Wellness, Eye Care. Each with category, items, doses, times, chain structure.

2. Create TemplateSearchBar: TextField with search icon, hint "Search templates...", 12px radius. Debounced onChanged callback.

3. Create CategoryFilterChips: Horizontal scroll of FilterChip/ChoiceChip widgets for each category. "All" selected by default. Selection updates provider filter.

4. Create TemplateCard: 120pt Card widget with icon circle (colored by category), title, tag pills, optional CourseTrackerBadge. InkWell → opens detail sheet.

5. Create CourseTrackerBadge: Small pill showing "Day X of Y" with amber background.

6. Create TemplateDetailSheet: showModalBottomSheet with DraggableScrollableSheet. Header (name, category, description), medicine list, chain preview, "Apply Template" + "Customize First" buttons.

7. Create template_providers.dart additions: searchQueryProvider, selectedCategoryProvider, filteredTemplatesProvider (combines search + category).

8. Build TemplateLibraryScreen: AppBar → TemplateSearchBar → CategoryFilterChips → GridView.count (crossAxisCount: 2) of TemplateCard.

9. Update AppRouter: Add /templates route. Not a shell branch (standalone page, accessed from FAB or deep link).

10. Update TemplatePickerScreen: Reuse TemplateCard widget, filtered by condition.

11. Widget tests: Verify grid renders, search filters, category filters, detail sheet opens.
    </steps>
