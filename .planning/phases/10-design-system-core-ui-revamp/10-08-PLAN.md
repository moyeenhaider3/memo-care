---
phase: 10-design-system-core-ui-revamp
plan: 08
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/features/reminders/presentation/add_reminder_screen.dart
  - lib/features/reminders/presentation/widgets/reminder_mode_toggle.dart
  - lib/features/reminders/presentation/widgets/reminder_type_grid.dart
  - lib/features/reminders/presentation/widgets/time_mode_toggle.dart
  - lib/features/reminders/presentation/widgets/day_of_week_pills.dart
  - lib/features/reminders/presentation/widgets/dose_unit_fields.dart
  - lib/features/reminders/presentation/widgets/notes_textarea.dart
  - lib/features/reminders/application/add_reminder_notifier.dart
  - lib/features/reminders/application/add_reminder_state.dart
  - lib/core/router/app_router.dart
autonomous: true
must_haves:
  truths:
    # --- Mode Toggle ---
    - ReminderModeToggle: Segmented control with "Say It" and "Build It" tabs
    - "Say It" tab: navigates to voice mode (placeholder until 12-03)
    - "Build It" tab: shows the manual form below
    - Default: "Build It" selected
    - Segmented control uses AppColors.accent for selected, grey for unselected
    # --- Reminder Type Grid ---
    - ReminderTypeGrid: 2x3 grid of type selection cards
    - Types: Medicine (pill icon, blue), Meal (restaurant icon, green), Activity (directions_run icon, orange), Call (phone icon, purple), Exercise (fitness_center icon, teal), Custom (edit icon, grey)
    - Selected type has accent border and light accent background
    - Each card: icon circle (40px) + label text (14pt)
    # --- Time Configuration ---
    - TimeModeToggle: "Fixed Time" / "Linked to Event" segmented toggle
    - Fixed Time: TimePicker input field
    - Linked to Event: Dropdown with anchors (Before Breakfast, After Lunch, etc.)
    - Offset input: "+/- minutes after event"
    # --- Day-of-Week Pills ---
    - DayOfWeekPills: Row of 7 circular pills (Mon-Sun)
    - Tappable to toggle individual days on/off
    - Selected: AppColors.accent fill + white text
    - Unselected: outlined grey
    - "Every Day" toggle above pills
    # --- Dose + Unit Fields ---
    - DoseUnitFields: Row with dose TextField (numeric) + unit dropdown (mg, ml, tablets, capsules, etc.)
    - Compact layout, 12px radius inputs
    # --- Notes ---
    - NotesTextarea: Multi-line TextField with "Additional notes..." placeholder
    - 3 lines visible, expandable
    - 12px radius
    # --- Form Layout ---
    - AddReminderScreen: AppBar ("Add Reminder") → ReminderModeToggle → ScrollView form body
    - Form body: ReminderTypeGrid → Medicine name TextField → DoseUnitFields → TimeModeToggle → DayOfWeekPills → NotesTextarea → "Add to Chain" optional toggle → "Save Reminder" button (56px, accent, full-width)
    - Validation: name required, dose required for medicine type, at least one day selected
    # --- State ---
    - AddReminderNotifier manages form state (type, name, dose, unit, time, days, notes, chainLink)
    - AddReminderState is a Freezed class with all form fields
    - Save creates reminder via existing ReminderRepository + schedules via AlarmScheduler
    # --- Route ---
    - Mounted at /add-reminder (standalone route from 10-01, accessed via FAB)
  artifacts:
    - lib/features/reminders/presentation/add_reminder_screen.dart — NEW (replaces placeholder)
    - lib/features/reminders/presentation/widgets/reminder_mode_toggle.dart — NEW
    - lib/features/reminders/presentation/widgets/reminder_type_grid.dart — NEW
    - lib/features/reminders/presentation/widgets/time_mode_toggle.dart — NEW
    - lib/features/reminders/presentation/widgets/day_of_week_pills.dart — NEW
    - lib/features/reminders/presentation/widgets/dose_unit_fields.dart — NEW
    - lib/features/reminders/presentation/widgets/notes_textarea.dart — NEW
    - lib/features/reminders/application/add_reminder_notifier.dart — NEW
    - lib/features/reminders/application/add_reminder_state.dart — NEW (Freezed)
    - lib/core/router/app_router.dart — UPDATED, /add-reminder renders AddReminderScreen
  key_links:
    - .planning/design-reference/add-reminder-manual.html — Primary visual spec
    - docs/MemoCare_UI_UX_Prompts.txt — P4 (Add Reminder, Build It section)
    - .planning/PLANNING_RULES.md — Rule 5 (missing features)
---

<objective>
Build the Add Reminder Manual form (Build It mode) with segmented mode toggle, reminder type grid, time configuration, day-of-week pills, dose/unit fields, and notes. The "Say It" tab is a placeholder for Voice Mode (Phase 12).

Reference: `add-reminder-manual.html` (Stitch) + P4 Build It section (docx)

Output: Complete manual add-reminder form with all input controls and save functionality.
</objective>

<execution_context>
@.planning/phases/10-design-system-core-ui-revamp/10-08-PLAN.md
</execution_context>

<context>
@.planning/design-reference/add-reminder-manual.html
@docs/MemoCare_UI_UX_Prompts.txt — P4

**Current state:** reminder_list_screen.dart exists but is a basic list. No add-reminder form exists (was a TODO stub). The /add-reminder route was created as placeholder in 10-01.

**Key build:**

- Full form screen with segmented Say It/Build It toggle
- 6-type reminder selection grid
- Fixed Time / Linked to Event time mode
- Day-of-week selection pills
- Dose + unit input fields
- Notes textarea
- Save flow → ReminderRepository → AlarmScheduler
  </context>

<steps>
1. Create AddReminderState (Freezed): reminderType, name, dose, unit, timeMode, fixedTime, linkedEvent, offsetMinutes, selectedDays, notes, chainLink. With initial factory.

2. Create AddReminderNotifier: Extends Notifier<AddReminderState>. Methods: setType, setName, setDose, setUnit, setTime, toggleDay, setNotes, toggleChainLink, save(). Save creates ReminderData, calls repository insert + alarm scheduling.

3. Create ReminderModeToggle: Segmented control ("Say It" / "Build It"). "Say It" shows coming-soon placeholder. "Build It" shows form.

4. Create ReminderTypeGrid: 2x3 grid. Each cell: icon circle + label. Selection state managed by notifier.

5. Create TimeModeToggle: Segmented "Fixed Time" / "Linked to Event". Fixed: time picker. Linked: anchor dropdown + offset.

6. Create DayOfWeekPills: 7 circular toggles (M/T/W/T/F/S/S). "Every Day" convenience toggle.

7. Create DoseUnitFields: Row with dose number input + unit dropdown.

8. Create NotesTextarea: Multi-line input with placeholder.

9. Build AddReminderScreen: AppBar → ModeToggle → ScrollView (TypeGrid → NameField → DoseUnit → TimeMode → DayPills → Notes → ChainToggle → SaveButton).

10. Update router: Wire /add-reminder to AddReminderScreen.

11. Widget tests: Verify form renders, type selection works, validation catches empty required fields.
    </steps>
