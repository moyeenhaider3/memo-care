---
phase: 10-design-system-core-ui-revamp
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/features/daily_schedule/presentation/home_screen.dart
  - lib/features/daily_schedule/presentation/widgets/next_pending_hero_card.dart
  - lib/features/daily_schedule/presentation/widgets/reminder_list_tile.dart
  - lib/features/daily_schedule/presentation/widgets/status_badge.dart
  - lib/features/daily_schedule/presentation/widgets/user_greeting_header.dart
  - lib/features/daily_schedule/presentation/widgets/progress_ring.dart
  - lib/features/daily_schedule/presentation/widgets/timeline_connector.dart
  - lib/features/daily_schedule/presentation/missed_reminders_sheet.dart
autonomous: true
must_haves:
  truths:
    # --- User Greeting Header ---
    - UserGreetingHeader shows circular avatar (48px) with user initial or default icon
    - UserGreetingHeader shows greeting text "Good morning, [Name]" (or afternoon/evening based on time)
    - Greeting text uses AppTypography.displayMedium (22pt SemiBold)
    - Right side shows notification bell icon (outlined)
    # --- Progress Ring ---
    - ProgressRing is a CustomPainter circular arc showing completed/total reminders
    - Ring shows "X/Y Done" in center with AppTypography.displayLarge for count
    - Ring color is AppColors.success for completed arc, AppColors.background for remaining
    - Ring size is 120px diameter
    # --- Navy Hero Card ---
    - NextPendingHeroCard has navy background (AppColors.primary) with white text
    - Shows medicine name, dosage, scheduled time in large white text
    - Two action buttons: "DONE" (AppColors.success, 56px height) and "SNOOZE" (amber outline, 56px height)
    - Card has 16px border radius (AppSpacing.cardRadius)
    - Card shadow matches AppShadows.elevated
    # --- Timeline Visualization ---
    - TimelineConnector widget renders vertical dotted line between reminder items
    - Dots are 4px circles, spaced 8px apart, in AppColors.skippedGrey
    - Active/current segment uses AppColors.accent dots
    - TimelineConnector has left-margin alignment matching list tile time column
    # --- Reminder List ---
    - ReminderListTile updated: colored left border strip (4px wide, color based on status: green=done, orange=pending, red=missed, grey=skipped)
    - Time column on left (14pt caption), medicine name center (17pt body), StatusBadge right
    - StatusBadge uses pill shape (chipRadius 999) with status-appropriate colors
    - Between each ReminderListTile, a TimelineConnector is rendered
    # --- Layout ---
    - HomeScreen scrollable Column: UserGreetingHeader → ProgressRing → NextPendingHeroCard → "Today's Schedule" header → Timeline list
    - If no reminders for today, show empty state illustration
    - MissedRemindersSheet still auto-triggers on app open if missed items exist
    # --- Design Tokens ---
    - All colors use AppColors from 10-01
    - All text styles use AppTypography from 10-01
    - All spacing/radius use AppSpacing from 10-01
  artifacts:
    - lib/features/daily_schedule/presentation/home_screen.dart — UPDATED, new layout with greeting + ring + timeline
    - lib/features/daily_schedule/presentation/widgets/user_greeting_header.dart — NEW
    - lib/features/daily_schedule/presentation/widgets/progress_ring.dart — NEW, CustomPainter ring
    - lib/features/daily_schedule/presentation/widgets/next_pending_hero_card.dart — UPDATED, navy design
    - lib/features/daily_schedule/presentation/widgets/reminder_list_tile.dart — UPDATED, timeline + border strip
    - lib/features/daily_schedule/presentation/widgets/status_badge.dart — UPDATED, pill shape
    - lib/features/daily_schedule/presentation/widgets/timeline_connector.dart — NEW
    - lib/features/daily_schedule/presentation/missed_reminders_sheet.dart — UPDATED, new design tokens
  key_links:
    - .planning/design-reference/home-dashboard.html — Primary visual spec
    - docs/MemoCare_UI_UX_Prompts.txt — P2 (Home Dashboard)
    - .planning/PLANNING_RULES.md — Rule 1 (merge both sources), Rule 2 (design tokens), Rule 13 (empty states)
---

<objective>
Revamp the Home Dashboard to match the Stitch design: user avatar + greeting header, circular progress ring, navy hero card with DONE/SNOOZE, and dotted timeline visualization between reminders.

Reference: `home-dashboard.html` (Stitch) merged with P2 from docx.

Output: Complete home screen revamp with all new widgets, using design tokens from 10-01.
</objective>

<execution_context>
@.planning/phases/10-design-system-core-ui-revamp/10-02-PLAN.md
</execution_context>

<context>
@.planning/design-reference/home-dashboard.html — Visual spec for Home Dashboard
@docs/MemoCare_UI_UX_Prompts.txt — P2: Home Dashboard behavioral specs

**What 10-01 provides:** AppColors, AppTypography, AppSpacing, AppShadows, 5-tab bottom nav, FAB

**Current home_screen.dart:** ConsumerStatefulWidget with NextPendingHeroCard (green/red buttons), chronological reminder list, missed sheet. Uses old teal theme.

**Key changes:**

- Add UserGreetingHeader with avatar + time-based greeting
- Add ProgressRing (CustomPainter circular progress)
- Redesign Hero Card: navy background with white text
- Add TimelineConnector: dotted vertical lines between items
- Add colored left border strip on each reminder tile
- Update StatusBadge to pill shape design
  </context>

<steps>
1. Create UserGreetingHeader widget: Row with CircleAvatar (48px, user initial or person icon) + Column (greeting text "Good morning/afternoon/evening, [Name]", date text). Use AppTypography and AppColors.

2. Create ProgressRing widget: CustomPainter that draws a circular arc. Props: completed count, total count. Center text "X/Y Done". Uses AppColors.success for progress arc.

3. Update NextPendingHeroCard: Change background to AppColors.primary (navy). White text for medicine name/dose/time. Update DONE button to AppColors.success bg. Update SNOOZE button to amber outline. Apply AppSpacing.cardRadius and AppShadows.elevated.

4. Create TimelineConnector widget: A column of small circles (4px) spaced 8px apart. Props: isActive (accent color vs grey), height. Used between list items.

5. Update ReminderListTile: Add 4px wide colored left border strip (Container with BoxDecoration). Integrate TimelineConnector. Update text styles to AppTypography. Status-based border color (green/orange/red/grey).

6. Update StatusBadge: Change to pill shape (borderRadius: AppSpacing.chipRadius). Smaller padding. Update colors to new palette.

7. Rebuild HomeScreen layout: ScrollableColumn → UserGreetingHeader → ProgressRing → NextPendingHeroCard → SizedBox → "Today's Schedule" SectionHeader → ListView.builder with ReminderListTile + TimelineConnector interleaved.

8. Add empty state: When no reminders, show centered illustration placeholder with "No reminders yet" text.

9. Update MissedRemindersSheet: Apply new design tokens (colors, typography, shadows, button styles).

10. Widget tests: Verify Home renders greeting, ring, hero card, timeline. Verify StatusBadge colors.
    </steps>
