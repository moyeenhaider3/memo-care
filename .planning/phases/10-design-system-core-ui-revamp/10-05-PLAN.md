---
phase: 10-design-system-core-ui-revamp
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/features/history/presentation/history_screen.dart
  - lib/features/history/presentation/widgets/week_selector_strip.dart
  - lib/features/history/presentation/widgets/compliance_donut_chart.dart
  - lib/features/history/presentation/widgets/day_grouped_log.dart
  - lib/features/history/presentation/widgets/history_card.dart
  - lib/features/history/presentation/widgets/export_pdf_button.dart
  - lib/features/history/presentation/widgets/history_empty_state.dart
  - lib/features/history/application/history_providers.dart
autonomous: true
must_haves:
  truths:
    # --- Week Selector ---
    - WeekSelectorStrip is a horizontal scrollable row of weekday cells (Mon-Sun)
    - Each cell shows day abbreviation + date number
    - Selected day/week is highlighted with AppColors.accent background
    - Arrows or swipe to navigate between weeks
    - Default selection: current week
    # --- Compliance Donut Chart ---
    - ComplianceDonutChart is a CustomPainter rendering a donut/ring chart
    - Segments: Done (green), Missed (red), Skipped (grey), Pending (amber)
    - Center text: overall compliance percentage (e.g., "87%") in displayLarge
    - Chart is 160px diameter
    - Legend below chart with colored dots + labels + counts
    # --- Day-Grouped Logs ---
    - DayGroupedLog groups confirmation records by date
    - Each date header: "Monday, Mar 8" with item count
    - Within each day: list of HistoryCard items
    # --- History Card ---
    - HistoryCard has colored status dot (left: green=done, red=missed, grey=skipped, orange=snoozed)
    - Shows: medicine name, scheduled time, actual confirmation time (if done), dosage
    - 16px border radius card with subtle shadow
    # --- Export ---
    - ExportPdfButton in AppBar actions: icon button (download/share icon)
    - Tapping exports selected week's data as PDF
    - Shows CircularProgressIndicator during generation
    - Opens share sheet when complete
    # --- Empty State ---
    - HistoryEmptyState shows calendar with leaf illustration (or placeholder), "No data yet for this week" text (from docx P8)
    # --- Layout ---
    - HistoryScreen: AppBar with title + ExportPdfButton → WeekSelectorStrip → ComplianceDonutChart → DayGroupedLog list
    - Scrollable body
    - All using design tokens from 10-01
  artifacts:
    - lib/features/history/presentation/history_screen.dart — UPDATED, complete redesign
    - lib/features/history/presentation/widgets/week_selector_strip.dart — NEW
    - lib/features/history/presentation/widgets/compliance_donut_chart.dart — NEW, CustomPainter
    - lib/features/history/presentation/widgets/day_grouped_log.dart — NEW
    - lib/features/history/presentation/widgets/history_card.dart — UPDATED, new design
    - lib/features/history/presentation/widgets/export_pdf_button.dart — NEW
    - lib/features/history/presentation/widgets/history_empty_state.dart — NEW
    - lib/features/history/application/history_providers.dart — UPDATED with week selection + compliance calculation
  key_links:
    - .planning/design-reference/history-compliance.html — Primary visual spec
    - docs/MemoCare_UI_UX_Prompts.txt — P8 (History & Compliance)
    - .planning/PLANNING_RULES.md — Rule 13 (empty states)
---

<objective>
Revamp the History & Compliance screen with week selector strip, compliance donut chart, export PDF, and day-grouped logs with colored status dots.

Reference: `history-compliance.html` (Stitch) + P8 (docx)

Output: Complete history screen with week navigation, visual compliance chart, grouped logs, and export.
</objective>

<execution_context>
@.planning/phases/10-design-system-core-ui-revamp/10-05-PLAN.md
</execution_context>

<context>
@.planning/design-reference/history-compliance.html
@docs/MemoCare_UI_UX_Prompts.txt — P8

**Current history_screen.dart:** Basic paginated list with medication filter bar. No week selector, no donut chart, no export, no day grouping.

**Key additions:**

- Week selector strip (horizontal scroll, navigate weeks)
- Compliance donut chart (CustomPainter ring chart with %)
- Day-grouped logs with colored status dots
- Export PDF button in AppBar
- Empty state illustration
  </context>

<steps>
1. Create WeekSelectorStrip: Horizontal scroll of 7 day cells. Selected week highlighted. Left/right arrows for week navigation. Update historyProvider with selected week filter.

2. Create ComplianceDonutChart: CustomPainter donut ring. Calculate done/missed/skipped/pending percentages from history data. Center percentage text. Legend row below.

3. Create DayGroupedLog: Groups history records by date. Date header with day name + count. Children are HistoryCard widgets.

4. Update HistoryCard: Add colored status dot (left). Show medicine name, time, confirmation details. New card design with 16px radius.

5. Create ExportPdfButton: AppBar action button. Generate PDF from week's data (use pdf package or basic CSV). Open share sheet. Loading state.

6. Create HistoryEmptyState: Calendar + leaf illustration placeholder. "No data yet for this week" text.

7. Rebuild HistoryScreen: AppBar (title + export button) → WeekSelectorStrip → ComplianceDonutChart → DayGroupedLog list → EmptyState when no data.

8. Update history providers: Add week selection state, compliance percentage calculation, day grouping transformation.

9. Widget tests: Verify donut chart renders, week selector navigates, empty state shows.
   </steps>
