---
phase: 11-kids-mode-ramadan-fasting
plan: 04
type: execute
wave: 2
depends_on: [11-03, 10-04]
files_modified:
  - lib/features/fasting/application/fasting_notifier.dart
  - lib/features/fasting/application/prayer_time_service.dart
  - lib/features/fasting/domain/prayer_times.dart
  - lib/features/fasting/data/ramadan_template_pack.dart
  - lib/features/templates/data/template_packs_data.dart
  - lib/features/daily_schedule/presentation/home_screen.dart
  - lib/features/daily_schedule/presentation/todays_full_schedule_screen.dart
  - lib/features/settings/presentation/widgets/fasting_toggle_tile.dart
  - lib/features/anchors/application/anchor_notifier.dart
  - lib/features/reminders/application/add_reminder_notifier.dart
  - lib/features/daily_schedule/presentation/widgets/hydration_counter.dart
  - lib/features/daily_schedule/application/hydration_notifier.dart
autonomous: true
must_haves:
  truths:
    # --- Fasting Toggle Integration ---
    - FastingToggleTile (from 10-04) wired to FastingNotifier
    - Toggle ON: activates Ramadan theme on /ramadan route, shows fasting-aware elements on existing screens
    - Toggle OFF: reverts to standard theme, hides Ramadan elements
    - Toggle state persisted to SharedPreferences
    # --- Prayer Time Calculation ---
    - PrayerTimeService: Calculates sehri/iftar times based on location
    - Uses location coordinates (latitude/longitude) + date
    - Algorithm: simple astronomical calculation or bundled lookup table for major cities
    - Fallback: manual time input (already in 11-03)
    - Output: PrayerTimes(fajr, sunrise, dhuhr, asr, maghrib, isha) — sehri = fajr - 10min, iftar = maghrib
    # --- Daytime Reminder Suppression ---
    - When fasting active: meal-linked reminders between sehri and iftar are auto-suppressed
    - Suppressed reminders show "Paused during fast" status in schedule
    - Medicine reminders (non-meal) still fire normally
    - AlarmScheduler skips scheduling for suppressed reminders
    # --- Ramadan Template Pack ---
    - New template pack: "Ramadan Medicine Pack"
    - Contains: morning medicines (before sehri), evening medicines (after iftar), taraweeh reminder
    - Added to template_packs_data.dart as 8th pack (Rule 9)
    # --- Impact on Existing Screens ---
    - Home Dashboard (10-02): When fasting active, hero card shows fasting-aware info ("Next: Iftar medicines at 6:45 PM")
    - Today's Full Schedule (10-03): Gold horizontal divider lines at sehri/iftar times
    - Templates (10-06): Include Ramadan Medicine Pack when fasting mode is on
    # --- Hydration Reminder ---
    - HydrationCounter widget: Shows daily water intake count (glasses/cups)
    - Increment button to log each glass
    - Target: configurable (default 8 glasses)
    - Shows on home dashboard below hero card
    - HydrationNotifier: Tracks daily count, resets at midnight, persists to DB
    - From Blueprint 3.2: "Hydration reminder + daily counter"
    # --- Anchor System Updates ---
    - AnchorNotifier updated: When fasting, breakfast/lunch anchors suppressed, sehri/iftar anchors override
    - Sehri anchor: maps to fajr prayer time - 10min
    - Iftar anchor: maps to maghrib prayer time
  artifacts:
    - lib/features/fasting/application/fasting_notifier.dart — UPDATED with toggle wiring + suppression logic
    - lib/features/fasting/application/prayer_time_service.dart — NEW
    - lib/features/fasting/domain/prayer_times.dart — NEW (Freezed)
    - lib/features/fasting/data/ramadan_template_pack.dart — NEW
    - lib/features/templates/data/template_packs_data.dart — UPDATED, adds Ramadan pack
    - lib/features/daily_schedule/presentation/home_screen.dart — UPDATED, fasting-aware hero card
    - lib/features/daily_schedule/presentation/todays_full_schedule_screen.dart — UPDATED, gold dividers
    - lib/features/settings/presentation/widgets/fasting_toggle_tile.dart — UPDATED, wired to notifier
    - lib/features/anchors/application/anchor_notifier.dart — UPDATED, sehri/iftar anchor overrides
    - lib/features/reminders/application/add_reminder_notifier.dart — UPDATED, suppression awareness
    - lib/features/daily_schedule/presentation/widgets/hydration_counter.dart — NEW
    - lib/features/daily_schedule/application/hydration_notifier.dart — NEW
  key_links:
    - .planning/PLANNING_RULES.md — Rule 9 (template packs, Ramadan = 8th), Rule 14 (impact on existing screens)
    - docs/MemoCare_UI_UX_Prompts.txt — P7 (Ramadan behavioral specs)
    - docs/MemoCare_Product_Blueprint.txt — Section 3.2 (hydration reminder)
---

<objective>
Wire fasting mode logic: settings toggle integration, prayer-time-aware scheduling, daytime meal reminder suppression, Ramadan Medicine Pack template, fasting-mode impact on existing screens (Home/Schedule/Templates), and hydration reminder counter.

Reference: P7 behavioral specs (docx) + Blueprint 3.2 (hydration) + Rule 14 (existing screen impact)

Depends on 11-03 (Ramadan screen must exist) and 10-04 (Settings with fasting toggle must exist).

Output: Fasting mode fully functional with toggle, auto-scheduling, suppression, template, and cross-screen impact.
</objective>

<execution_context>
@.planning/phases/11-kids-mode-ramadan-fasting/11-04-PLAN.md
</execution_context>

<context>
@.planning/PLANNING_RULES.md — Rule 9, Rule 14
@docs/MemoCare_UI_UX_Prompts.txt — P7
@docs/MemoCare_Product_Blueprint.txt — Section 3.2

**11-03 provides:** Ramadan screen with UI (countdown cards, progress bar, medicine sections), RamadanTheme, FastingNotifier, FastingState.
**10-04 provides:** Settings screen with FastingToggleTile (UI only, not wired).

**This plan wires everything together:**

1. Fasting toggle → activates/deactivates Ramadan features
2. Prayer time calculation → auto-sets sehri/iftar times
3. Daytime meal-linked reminders suppressed during fast
4. Sehri/iftar replace breakfast/lunch anchors
5. Ramadan Medicine Pack added to template library
6. Home/Schedule screens show fasting-aware content
7. Hydration counter on home screen
   </context>

<steps>
1. Create PrayerTimes (Freezed): fajr, sunrise, dhuhr, asr, maghrib, isha as DateTime fields.

2. Create PrayerTimeService: Calculate prayer times from lat/lng + date. Use simplified calculation or bundled city data. Compute sehri = fajr - 10min, iftar = maghrib.

3. Wire FastingToggleTile: onChanged → FastingNotifier.toggleFastingMode(). Persist state. When enabled: trigger prayer time calculation, set sehri/iftar times.

4. Implement daytime suppression in alarm scheduling: When fasting active, check if reminder is meal-linked AND falls between sehri and iftar → skip scheduling → mark as "Paused during fast".

5. Update AnchorNotifier: When fasting, breakfast anchor → sehri time, lunch anchor → suppressed, dinner anchor → iftar time. Cascade recalculation.

6. Create Ramadan Medicine Pack: Pre-sehri medicines, post-iftar medicines, taraweeh time reminder. Add to template_packs_data.dart.

7. Update Home Dashboard: Conditionally show fasting hero info when fasting active. Show link to /ramadan screen.

8. Update Today's Full Schedule: Add gold horizontal divider lines at sehri and iftar times when fasting active.

9. Create HydrationCounter: Widget showing glass icon + count + increment button. Target display.

10. Create HydrationNotifier: Tracks daily water intake. Reset at midnight. Persist to SharedPreferences or Drift.

11. Add HydrationCounter to home screen (below hero card, above schedule list).

12. Unit tests: Prayer time calculation, suppression logic, anchor override, template, hydration counter.
    </steps>
