import 'package:flutter/material.dart';

/// Onboarding step: request system permissions (alarm, notification).
///
/// Full implementation in Plan 06-07.
class PermissionStep extends StatelessWidget {
  const PermissionStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Permission Step — coming in Plan 06-07'),
    );
  }
}
