import 'package:flutter/material.dart';

/// Onboarding step: select medical condition.
///
/// Full implementation in Plan 06-05.
class ConditionStep extends StatelessWidget {
  const ConditionStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Condition Step — coming in Plan 06-05'),
    );
  }
}
