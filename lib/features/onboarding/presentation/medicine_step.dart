import 'package:flutter/material.dart';

/// Onboarding step: customise medicines.
///
/// Full implementation in Plan 06-06.
class MedicineStep extends StatelessWidget {
  const MedicineStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Medicine Step — coming in Plan 06-06'),
    );
  }
}
